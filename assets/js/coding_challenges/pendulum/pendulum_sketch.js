import p5 from 'p5';
import * as engine from './engine';

function pendulumSketch(p5) {
  p5.setup = function () {
  };

  p5.draw = function () {
  };
}

export default new p5(pendulumSketch, 'pendulum-container');